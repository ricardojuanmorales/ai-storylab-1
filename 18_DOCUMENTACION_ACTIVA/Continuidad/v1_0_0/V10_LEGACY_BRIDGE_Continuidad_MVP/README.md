# V10-LEGACY-BRIDGE

Huella pública, snapshot histórico, puente de respaldo y pruebas de namespace. Ninguna ruta fue desplegada.

## Recuperación arquitectónica R3

El contrato compartido pasó a `ports`; la inspección de almacenamiento permanece en `adapters`. Audit y verify locales pasaron.

## Recuperación de rename y allowlist R4

El contrato compartido pasó a `ports`; la inspección de almacenamiento permanece en `adapters`. Audit y verify locales pasaron.
